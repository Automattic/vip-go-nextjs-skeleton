#import "../fragments/ContentNode.graphql"

query ContentNodes(
	$contentTypes: [ContentTypeEnum]
	$slug: String
) {
	contentNodes(
		where: {
			contentTypes: $contentTypes
			name: $slug
		}
	) {
		nodes {
			...ContentNodeView
		}
	}
}
