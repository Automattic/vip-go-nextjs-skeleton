#import "../fragments/DisplayNode.graphql"

query DisplayNodes(
	$contentTypes: [DisplayContentTypeEnum]
	$slug: String
) {
	displayNodes(
		where: {
			contentTypes: $contentTypes
			name: $slug
		}
	) {
		nodes {
			...DisplayNodeFields
		}
	}
}
